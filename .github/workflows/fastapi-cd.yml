name: Deploy FastAPI Backend

on:
  push:
    branches: [ main ]
    paths:
      - 'Backend/**'
      - '.github/workflows/fastapi-cd.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
        cache-dependency-path: 'Backend/requirements.txt'
    
    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y unixodbc-dev
    
    - name: Install dependencies
      run: |
        cd Backend
        pip install -r requirements.txt
    
    - name: Run tests
      run: |
        cd Backend
        if [ -d "tests" ]; then
          python -m pytest tests/ -v
        else
          echo "No tests directory found"
        fi
    
    - name: Validate FastAPI app
      run: |
        cd Backend
        python -c "
        try:
            from fastapi_app import app
            print('✅ FastAPI app can be imported successfully')
        except Exception as e:
            print(f'❌ Import error: {e}')
            exit(1)
        "

  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: success()
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Deploy to EC2
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script_stop: true
        timeout: 300s
        script: |
          set -e
          
          DEPLOY_USER=ubuntu
          APP_DIR="/var/www/fastapi-app"
          BACKUP_DIR="/var/www/backups"
          
          # -----------------------------------------------------------
          # 1. Install System Drivers
          # -----------------------------------------------------------
          if ! dpkg -s unixodbc-dev >/dev/null 2>&1; then
              echo "🔧 Installing System Dependencies..."
              sudo apt-get update
              sudo apt-get install -y unixodbc-dev gcc g++ python3-dev
          fi
          
          # -----------------------------------------------------------
          # 2. Fix Permissions & Clean Broken State
          # -----------------------------------------------------------
          sudo mkdir -p "$BACKUP_DIR"
          
          if [ -d "$APP_DIR/HealthGuard-AI" ]; then sudo rm -rf "$APP_DIR"; fi
          if [ -d "$APP_DIR" ] && [ ! -d "$APP_DIR/.git" ]; then sudo rm -rf "$APP_DIR"; fi
          
          sudo mkdir -p "$APP_DIR"
          sudo chown -R $DEPLOY_USER:$DEPLOY_USER "$APP_DIR"
          sudo chown -R $DEPLOY_USER:$DEPLOY_USER "$BACKUP_DIR"
          
          cd "$APP_DIR"
          
          # -----------------------------------------------------------
          # 3. Clone or Pull
          # -----------------------------------------------------------
          git config --global --add safe.directory "$APP_DIR"
          
          if [ ! -d ".git" ]; then
            echo "📥 Cloning repository..."
            git clone https://github.com/Ahsanjarral009/HealthGuard-AI.git .
          else
            echo "📥 Pulling latest changes..."
            git fetch origin
            git reset --hard origin/main
          fi
          
          # -----------------------------------------------------------
          # 4. Setup Python Environment
          # -----------------------------------------------------------
          if [ ! -d "venv" ]; then 
            echo "🐍 Creating Python virtual environment..."
            python3 -m venv venv
          fi
          
          source venv/bin/activate
          pip install --upgrade pip setuptools wheel
          
          echo "📦 Installing dependencies..."
          pip install -r Backend/requirements.txt
          
          # -----------------------------------------------------------
          # 5. Setup .env file
          # -----------------------------------------------------------
          if [ -f ".env.example" ]; then
            cp .env.example Backend/.env
            cp .env.example .env
          fi
          
          # -------------------------------------------------------------
          # 6. Create Wrapper Script (start.sh)
          # This guarantees the environment loads correctly every time.
          # -------------------------------------------------------------
          echo "🔧 Creating startup wrapper script..."
          cd "$APP_DIR/Backend"
          
          # We use printf to write the script safely
          printf "#!/bin/bash\ncd $APP_DIR/Backend\nsource $APP_DIR/venv/bin/activate\nexport PYTHONPATH=$APP_DIR/Backend\nexec python -m uvicorn fastapi_app:app --host 0.0.0.0 --port 8000\n" > start.sh
          
          chmod +x start.sh
          
          # -------------------------------------------------------------
          # 7. Create Systemd Service
          # -------------------------------------------------------------
          echo "🔧 Creating systemd configuration..."
          
          # We point ExecStart to our new wrapper script
          # This bypasses all Systemd environment complexities
          SERVICE_CONTENT="[Unit]
          Description=FastAPI Application
          After=network.target
          
          [Service]
          User=$DEPLOY_USER
          Group=$DEPLOY_USER
          WorkingDirectory=$APP_DIR/Backend
          ExecStart=$APP_DIR/Backend/start.sh
          Restart=always
          RestartSec=5
          StandardOutput=journal
          StandardError=journal
          
          [Install]
          WantedBy=multi-user.target"
          
          # Write safely
          echo "$SERVICE_CONTENT" | sudo tee /etc/systemd/system/fastapi.service > /dev/null
          
          # -------------------------------------------------------------
          # 8. Reload and Start
          # -------------------------------------------------------------
          echo "🔄 Reloading systemd..."
          sudo systemctl daemon-reload
          sudo chmod 644 /etc/systemd/system/fastapi.service
          
          echo "🚀 Starting service..."
          sudo systemctl enable fastapi.service
          sudo systemctl restart fastapi.service
          
          echo "⏳ Waiting 10s for service startup..."
          sleep 10
          
          # -------------------------------------------------------------
          # 9. Health Check (Simple Loop)
          # -------------------------------------------------------------
          echo "🏥 Performing health check..."
          
          for i in 1 2 3 4 5; do
              echo "Attempt $i/5..."
              if curl -f -s http://localhost:8000/health > /dev/null 2>&1; then
                  echo "✅ Health check passed!"
                  exit 0
              fi
              
              if [ "$i" -eq "5" ]; then
                  echo "❌ Health check failed after 5 attempts"
                  echo "🔍 Last 100 lines of logs:"
                  sudo journalctl -u fastapi.service -n 100 --no-pager
                  exit 1
              fi
              
              echo "⏳ Waiting 5 seconds..."
              sleep 5
          done

    - name: Send deployment notification
      if: always()
      run: |
        if [ "${{ job.status }}" = "success" ]; then
          echo "✅ Deployment successful!"
        else
          echo "❌ Deployment failed!"
        fi
